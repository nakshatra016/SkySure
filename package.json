{
  "name": "skysure-root",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend"
  }
}
